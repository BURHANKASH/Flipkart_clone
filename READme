clone the repo link 
cd Server
Hit npm i
Hit npm start
//////////////////////////////
open the new terminal
cd client
npm i 
hit npm start


/////////////////////////////

create .env file in the Server to add the varaibles
DB_USERNAME
DB_PASSWORD
